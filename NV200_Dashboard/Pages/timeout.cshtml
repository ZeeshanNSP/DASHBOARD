@page
@model NV200_Dashboard.Pages.timeoutModel
@{
    
}

<script>
$(document).ready(function() {
    $("body").css("background","url('../assets/timeout.jpg')")
    $("body").css("background-size","cover")
    let d = JSON.parse(localStorage.getItem("obj"))
    let data = {
        "amount":`${d["amount"]}`,
        "mobile":d["number"],
        "service":d["service"],
        "remaining":`${d["remaining"]}`
    }
    //console.log(data)
    
    $.get("https://10.39.13.49:443/jwpTimeOut", data, function(data) {
        window.location.href="/"
    })
    
})
</script>